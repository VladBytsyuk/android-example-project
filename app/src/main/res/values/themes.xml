<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.AndroidExampleProject.Launcher" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/img_window_background_day</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/day_primary</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.AndroidExampleProject" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/day_primary</item>
        <item name="colorPrimaryVariant">@color/day_primary_variant</item>
        <item name="colorOnPrimary">@color/day_on_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/day_on_secondary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/black</item>
    </style>
</resources>